{
  "name": "rest",
  "version": "1.0.0",
  "description": "rest para o app de doacao de animais",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare" : "rm ./src/database/*.* && npx knex migrate:down --all && npx knex migrate:latest ",
    "start": "nodemon ./src/index"
  },
  "author": "Lino Mota & Gabriel Meneses",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "knex": "^0.20.13",
    "nodemon": "^2.0.2",
    "sqlite3": "^4.1.1",
    "uuid": "^7.0.3"
  }
}
